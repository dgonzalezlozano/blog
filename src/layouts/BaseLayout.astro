---
import '../styles/global.css';

import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import HeaderBase from '../components/HeaderBase.astro';

const { pageTitle, title, pubDate, author, tags, description, subTitle, keywords } = Astro.props;
const blogTitle = 'domin.es';
---
<!DOCTYPE html>
<html lang="es">
    <head>
        <HeaderBase 
            pageTitle={pageTitle}
            subTitle={subTitle}
            description={description}
            keywords={keywords}
            author={author}
        />
    </head>
    <body class="flex flex-col min-h-screen bg-gray-100">
        <Header title={title} pubDate={pubDate} author={author} tags={tags} description={description} subTitle={subTitle}/>
        <main class="flex-grow">
            <slot />
        </main>
        <Footer />
        <!--<script>
            import { run } from 'vanilla-cookieconsent';
            import { config } from '../components/CookieConsentConfig';
            document.body.classList.add('cc--elegant-black');
            run(config);
        </script>-->
    </body>
</html>